import { Paper, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Chip } from "@mui/material";
const labels:any={DEALER_PURCHASE:"Agencia / proveedor",PRIVATE_PURCHASE:"Particular",TRADE_IN:"Parte de pago",CONSIGNMENT:"Consignado"};
export function PurchaseTable({ purchases }: { purchases: any[] }) {
 return <TableContainer component={Paper} sx={{mt:2,borderRadius:2}}><Table><TableHead><TableRow><TableCell>#</TableCell><TableCell>Fecha</TableCell><TableCell>Tipo</TableCell><TableCell>Vehículo</TableCell><TableCell>Origen / contraparte</TableCell><TableCell>Venta asociada</TableCell><TableCell align="right">Valor</TableCell></TableRow></TableHead><TableBody>
 {purchases.length?purchases.map(p=><TableRow key={p.id} hover><TableCell>{p.id}</TableCell><TableCell>{p.createdAt?new Date(p.createdAt).toLocaleDateString("es-AR"):"-"}</TableCell><TableCell><Chip size="small" label={labels[p.acquisitionType]||p.acquisitionType}/></TableCell><TableCell>{p.vehicle?`${p.vehicle.brand??""} ${p.vehicle.model??""} ${p.vehicle.year??""} (${p.vehicle.plate??""})`:"-"}</TableCell><TableCell>{p.supplierName || (p.client?`${p.client.firstName??""} ${p.client.lastName??""}`.trim():"-")}</TableCell><TableCell>{p.relatedSaleId?`#${p.relatedSaleId}`:"-"}</TableCell><TableCell align="right">$ {Number(p.amount||0).toLocaleString("es-AR")}</TableCell></TableRow>):<TableRow><TableCell colSpan={7} align="center">No hay adquisiciones registradas aún.</TableCell></TableRow>}
 </TableBody></Table></TableContainer>;
}
